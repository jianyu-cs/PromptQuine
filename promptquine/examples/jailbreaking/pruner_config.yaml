defaults:
  - _self_

data:
  dataset: Advbench

model:
  name: "lmsys/vicuna-7b-v1.5"
  ICL_shots: 2
  ICL_index: 0
  priming: true
  # Ray parameters
  gpus_per_bundle: 1
  cpus_per_bundle: 4

prompt: # Evaluation-Only
  prompt: ""
  is_pruned_prompt: true 

pruning:
  algorithm: "TAPruning"  # TAPruning | PromptQuine
  fix_prune_order: true
  pruning_order_seed: 0
  TAPruning_threshold: 0.96 # Only TAPruning
  pruning_metric: "ASR-EM" # "ASR-LLM", "ASR-SV"
  # Evaluation-only
  selection_top_k: 1 # Only TAPruning

prompt_quine: # Default
  initialize_duplicate: true
  min_prompt_length: 15
  max_num_prompts: 10000
  algorithm_mode: "GGA"
  population_size: 30
  reproduction_size: 50
  # Evaluation-only
  top_percent_rerank: 10
  test_all_elites_for_debug: false